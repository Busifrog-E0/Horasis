

const get_users_UserId_connections = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const post_connections_send = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const patch_connections_accept = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const delete_connections_reject = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const delete_connections_cancel = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const delete_connections = async (req, res, next) => {
    // #swagger.tags = ['Connections']

    return next();
}

const get_Connections_Count = async (req, res, next) => {
    // #swagger.tags = ['Connections']
    /* #swagger.responses[200] = {
             description: 'User Data',
             schema:  5 
     } 
 */
    next();
}


export default {
    get_users_UserId_connections,
    post_connections_send,
    patch_connections_accept,
    delete_connections_reject,
    delete_connections_cancel,
    delete_connections,
    get_Connections_Count,
}